{
  "name": "shift-board-no-auth",
  "version": "1.0.0",
  "description": "Shift Board — containerized backend. Now includes System ID + password login for a dev-curated admin allowlist (see README's Authentication section) — the 'no-auth' in the name is historical at this point, kept so existing deployment docs/scripts referencing it don't break.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6"
  }
}
